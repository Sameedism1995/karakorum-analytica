# Web scraping stack for Karakorum Analytica (install: pip install -r requirements-scraper.txt)
# Base HTTP (also in requirements-api.txt — listed here for a complete scraper env)
httpx[http2]
requests

# HTML/XML parsing
beautifulsoup4
lxml
selectolax

# TLS / browser fingerprint (helps with basic bot detection on static HTTP)
curl_cffi

# JavaScript-rendered pages (run after install: playwright install chromium)
playwright

# Politeness & resilience
tenacity
protego
feedparser
fake-useragent
charset-normalizer

# Optional: third-party scrape proxy (key already in app.config SCRAPER_API_KEY)
# Uses plain requests/httpx — no extra package required.
