# Copy to Streamlit Cloud → App Settings → Secrets (not for local use unless needed)
# Replace with your live Render API URL after deploying the backend.

API_BASE_URL = "https://karakorum-analytica-api.onrender.com"
